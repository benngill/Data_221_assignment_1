# Data_221_assignment_1
Assignment 1 Ben Gill

Question_1: file takes a threshold (int), multiplies consecutive integers n+1 until threshold is exceeded. Prints what integer exceeded what threshold

Question_2: function that takes a list of strings, returns each string as dict key, with string length and parity as value

Question_3: function that computes x^y and creates a list of x,y pairs, excluding negative exponents y

Question_4: sorts a random list, prints sorted list, indices > x, and first matchin index

Question_5: function that takes two radii, and checks if they are both positive ints. Computes the areas of each circle, and then finds the percent difference between them

Question_6: function that takes a list of numbers and returns a dictionary where each key is a number, and the value is the % of indices that are <= to that key

Question_7: function that converts seconds since midnight to 12 hour standard time

Question_8: creates a data.frame from provided data, and creates a new column from existing data
